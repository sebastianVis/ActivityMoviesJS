import { dataJson } from "./db";

export function search(){
    const busqueda = document.getElementById('box').toLowerCase(); // todo el texto en minuscula;
    const resultado = document.getElementById('movieList');
    resultado.innerHTML ='';
}

if (busqueda){
    const filtro = dataJson.
}